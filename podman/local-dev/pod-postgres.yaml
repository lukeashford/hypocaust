apiVersion: v1
kind: Pod
metadata:
  name: hypocaust-postgres
spec:
  volumes:
    - name: postgres_data
      hostPath:
        path: ${HOSTPATH}/podman/volumes/postgres
  containers:
    - name: postgres
      image: pgvector/pgvector:pg17
      envFrom:
        - configMapRef:
            name: hypocaust-dev
      volumeMounts:
        - mountPath: /var/lib/postgresql/data:Z
          name: postgres_data
          readOnly: false
      ports:
        - containerPort: 5432
          hostPort: 7888
          protocol: TCP
    - name: pgadmin
      image: docker.io/dpage/pgadmin4:2024-12-09-2
      envFrom:
        - configMapRef:
            name: hypocaust-dev
      ports:
        - containerPort: 80
          hostPort: 7889
          protocol: TCP
