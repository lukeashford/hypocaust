package com.example.web.dto;

/**
 * Enum representing asset types for visual assets Matches TypeScript union type: 'image' Can be
 * extended for future asset types (video, audio, etc.)
 */
public enum AssetType {
  IMAGE
}